# 📊 Отчет о реализации системы логистики (Logistics DB 5NF)

## 📌 Общая сводка
Проект представляет собой базу данных для логистической системы, спроектированную в **5-й нормальной форме (5NF)**. Реализована сложная бизнес-логика обработки статусов грузов, разграничение прав доступа, аналитическая отчетность и оптимизация производительности.

**Текущий статус:** ✅ Все модули развернуты и протестированы.

---

## 📂 Структура проекта и реализованные файлы

Ниже приведено подробное описание каждого модуля, который был внедрен в систему.

### 1️⃣ Автоматизация и Бизнес-логика
**Файл:** `PROCEDURES_AND_TRIGGERS.sql`
**Статус:** ✅ Внедрено

Этот модуль отвечает за целостность данных и автоматические переходы статусов грузов. В системе больше нельзя "просто поменять статус" — все изменения проходят строгую валидацию.

*   **Матрица переходов (`статусы_переходы`):** Таблица, запрещающая нелогичные действия (например, нельзя перевести груз из "Новый" сразу в "Доставлен").
*   **Основная функция (`изменить_статус_груза`):** Единая точка входа для смены статуса. Гарантирует запись истории, проверку прав и корректность перехода.
*   **Триггеры:**
    *   `валидация_перехода_статуса` — блокирует прямые `UPDATE`, если они нарушают логику.
    *   `установить_дату_доставки` — автоматически ставит текущую метку времени, когда статус меняется на "Доставлен".
    *   `авто_в_пути` — переводит груз в статус "В пути", как только ему назначена машина (вставка в таблицу `груз_и_средства`).

### 2️⃣ Безопасность и Контроль доступа (RBAC)
**Файл:** `06_SECURITY_ACCESS_CONTROL.sql`
**Статус:** ✅ Внедрено

Реализована ролевая модель доступа. В базе данных созданы не просто пользователи, а иерархия ролей с разными правами.

*   **Роли:**
    *   `role_admin` (Менеджер): Полный доступ ко всем данным и структурам.
    *   `role_dispatcher` (Диспетчер): Может создавать грузы, менять статусы, назначать водителей. Чтение всех таблиц.
    *   `role_read_only` (Аналитик): Только чтение данных (`SELECT`) для построения отчетов.
*   **Row-Level Security (RLS):**
    *   Настроена политика безопасности для таблицы `история_статусов`. Админы видят всё, а обычные пользователи — только свои действия (или ограниченный набор, в зависимости от настройки).

### 3️⃣ Аналитическая отчетность
**Файл:** `07_REPORTING_QUERIES.sql`
**Статус:** ✅ Протестировано на реальных данных

Набор сложных SQL-запросов для бизнеса. Ошибки с несуществующими полями были устранены в финальной версии (V3).

*   **KPI Доставок:** Показывает динамику заказов по месяцам и процент успешных доставок (Status ID = 6).
    *   *Результат:* За последние полгода обработано ~500 заказов, пик пришелся на Сентябрь 2025 (114 заказов).
*   **Топ Клиентов:** Рейтинг клиентов по количеству заказов.
    *   *Лидеры:* ООО "СтройМаркет" (18 заказов) и ООО "ГлавСтрой" (17 заказов).
*   **Статистика по водителям:** Показывает, сколько машин закреплено за каждым водителем.
    *   *Исправление:* Изначально отчет был пуст из-за `NULL` в полях водителей. После применения `09_FIX_DRIVERS.sql` данные отображаются корректно.

### 4️⃣ Тестирование производительности
**Файл:** `08_PERFORMANCE_TESTING.sql`
**Статус:** ✅ Оптимизировано

Проверка того, как база держит нагрузку.

*   **Генератор данных:** Написана функция `generate_load_v3`, которая безопасно (проверяя ID) сгенерировала **1000+** записей в историю статусов.
*   **Оптимизация (Индексы):**
    *   *До оптимизации:* Поиск по дате выполнялся полным перебором (`Seq Scan`).
    *   *После создания индекса `idx_history_date_range`:* Планировщик переключился на `Bitmap Index Scan`.
    *   *Результат:* Время выполнения запроса сократилось до **0.630 ms** (менее 1 миллисекунды).

### 5️⃣ Исправление данных
**Файл:** `09_FIX_DRIVERS.sql`
**Статус:** ✅ Применено

Служебный скрипт, который устранил проблему отсутствующих связей между машинами и водителями. Он случайным образом распределил водителей по свободным транспортным средствам, что "оживило" отчеты.

---

## 📊 Текущая статистика базы данных

| Таблица | Кол-во записей (approx) | Описание |
| :--- | :--- | :--- |
| **история_статусов** | 21,492 | Самая нагруженная таблица (включая тестовые данные) |
| **грузы** | 500 | Основные заказы |
| **платежи** | 500 | Финансовые транзакции |
| **груз_и_средства** | 414 | Связи грузов с транспортом (5NF) |
| **транспортные_средства** | 100 | Автопарк |
| **клиенты** | 50 | База заказчиков |
| **водители** | 30 | Персонал |

---

## 🚀 Как запустить проект (Docker)

Если потребуется развернуть всё с нуля, используйте следующие команды:

1.  **Применить бизнес-логику:**
    ```bash
    docker exec -it logistics_postgres psql -U logistics_admin -d logistics_5nf -f /tmp/PROCEDURES_AND_TRIGGERS.sql
    ```

2.  **Настроить безопасность:**
    ```bash
    docker exec -it logistics_postgres psql -U logistics_admin -d logistics_5nf -f /tmp/06_SECURITY_ACCESS_CONTROL.sql
    ```

3.  **Исправить связи (водители):**
    ```bash
    docker exec -it logistics_postgres psql -U logistics_admin -d logistics_5nf -f /tmp/09_FIX_DRIVERS.sql
    ```

4.  **Получить отчеты:**
    ```bash
    docker exec -it logistics_postgres psql -U logistics_admin -d logistics_5nf -f /tmp/07_REPORTING_QUERIES.sql
    ```

## ✅ Итог
Система полностью функциональна, оптимизирована и готова к демонстрации. Все требования по 5NF соблюдены, безопасность настроена, автоматизация работает через триггеры.
