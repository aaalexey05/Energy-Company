# Logistics 5NF - Система управления логистикой

## 🚀 Быстрый старт

### 1. Структура проекта
```
logistics-db/
├── docker-compose.yml
├── Dockerfile (для PostgreSQL)
├── Makefile
├── sql-scripts/           # SQL скрипты инициализации БД
├── backups/               # Резервные копии
└── webapp/
    ├── Dockerfile
    ├── requirements.txt
    ├── app.py
    ├── config.py
    ├── models.py
    ├── auth.py
    ├── routes/
    │   ├── __init__.py
    │   ├── admin.py
    │   ├── manager.py
    │   ├── warehouse.py
    │   ├── courier.py
    │   └── client.py
    ├── static/
    │   ├── css/style.css
    │   └── js/main.js
    └── templates/
        ├── base.html
        ├── login.html
        ├── admin/
        ├── manager/
        ├── warehouse/
        ├── courier/
        └── client/
```

### 2. Первый запуск
```bash
# Инициализация всего окружения
make init

# Или поэтапно:
make build    # Собрать образы
make up       # Запустить контейнеры
make check    # Проверить состояние БД
```

### 3. Доступ к сервисам
- 🌐 **Веб-интерфейс**: http://localhost:5000
- 📊 **PostgreSQL**: localhost:5432
- 🔧 **pgAdmin**: http://localhost:5050

### 4. Тестовые пользователи
После инициализации создайте пользователей:
```bash
make create-user
```

Логин: `admin@logistics.ru`  
Пароль: `admin123`

### 5. Основные команды

```bash
make help          # Показать все команды
make up            # Запустить
make down          # Остановить
make logs-webapp   # Логи веб-приложения
make psql          # Консоль PostgreSQL
make backup        # Создать бэкап БД
make status        # Статус контейнеров
```

## 📝 Разработка

### Режим разработки
```bash
make dev           # Сборка + логи в реальном времени
```

### Пересборка только webapp
```bash
make rebuild-webapp
```

### Просмотр логов
```bash
make logs-webapp   # Логи Flask
make logs          # Логи PostgreSQL
make logs-all      # Все логи
```

## 🔧 Роли в системе

1. **Администратор** - полный доступ, управление пользователями, отчёты
2. **Менеджер по заказам** - управление грузами, аналитика
3. **Кладовщик** - управление складом, отгрузки
4. **Курьер** - доставки, обновление статусов
5. **Клиент** - отслеживание грузов

## 💾 Резервное копирование

```bash
# Создать бэкап
make backup

# Восстановить из последнего бэкапа
make restore
```

## 🧹 Очистка

```bash
# Остановить контейнеры (данные сохраняются)
make clean

# УДАЛИТЬ ВСЁ (включая данные!)
make clean-all
```

## 📚 Документация БД

База данных приведена к **5-й нормальной форме (5NF)**.

### Основные таблицы:
- `грузы` - информация о грузах
- `клиенты` - клиенты
- `сотрудники` - пользователи системы
- `роли` - роли доступа
- `статусы_заказов` - статусы грузов
- `склады` - склады
- `города` - справочник городов

### Хранимые процедуры:
- `изменить_статус_груза()` - изменение статуса с логированием

## ⚠️ Важно

- Пароли в production должны быть изменены!
- `SECRET_KEY` должен быть уникальным
- Не коммитьте `.env` файлы с реальными паролями


admin / admin123

manager / admin123

logist / admin123

warehouse / admin123